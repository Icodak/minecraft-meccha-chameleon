# meccha:trigger/eyedropper  (advancement reward, runs as the player)
# PILLAR 3.3 - revoke immediately so the advancement can re-fire forever,
# then run the eyedropper cast from the player's eyes.
# say eyedropper
execute at @s anchored eyes run function meccha:eyedropper/cast
